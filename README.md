# 🐍 Python Intern Task

This project is a small Python script that fetches user data from a public API and displays it in a clean, readable format.

---

## 🚀 Features
- Uses the **GET** method to fetch data from a public API.
- Displays:
  - Name  
  - Username  
  - Email  
  - City
- (Bonus) Prints only users whose city starts with the letter **'S'**.
- Handles API errors gracefully.

---

## 🔧 Requirements
- Python 3.7 or higher
- `requests` library

Install the library by running:
```bash
pip install requests
